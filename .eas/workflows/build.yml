name: Build on Push

on:
  workflow_dispatch: {}


jobs:
  build_android:
    name: Build Android App
    type: build
    params:
      platform: android
      profile: preview

  build_ios:
    name: Build iOS App
    type: build
    params:
      platform: ios
      profile: preview